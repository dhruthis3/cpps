#include <stdio.h>
int input(void)
{
   int a,b,c;
   printf("Enter two numbers\n");
   scanf("%d%d",&a &b)
   return abc;
}
int compute(int x)
{
  c=a+b;
  return a,b;
}
void output(int m, int n)
{
  printf("%d%d+1 is %d%d",m,n);
}
int main(void)
{
  int a=input();
  int b=input();
  int c=compute(a,b);
  output(a,b,c);
}
